{{-- <x-app-layout>
    <x-slot name="header">
        <h2 class="font-semibold text-xl text-gray-800 leading-tight">
            {{ __('Dashboard') }}
        </h2>
    </x-slot>
    <div class="p-6 sm:px-20 bg-base-200">
        @if ($availableCars->isEmpty())
            <p>No cars available.</p>
        @else
            <ul>
                @foreach ($availableCars as $car)
                    <li>{{ $car->name }}</li>
                @endforeach
            </ul>
        @endif
    </div>

</x-app-layout> --}}
olla!
